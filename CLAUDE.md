# freecrypto.net — operational pins

> ## 🚀 ACTIVE — building an A-Ads "honest free-crypto" content/utility hub (decided 2026-09-20). SELL freeze LIFTED.
> **Un-froze after 30 days with ZERO bids on Afternic.** Tim: keep the domain, build a **complete website** (his explicit call, overriding the panel's caution). **Referral is confirmed DEAD as an income source** — first-party FaucetPay data: ref `r/301005` has run since 2020-05-17 and earned **$0.03 from 45 signups** (`docs/superpowers/research/faucetpay-telemetry.md`). Rates: PTC/Staking 50%, offerwall 15%, faucet claims ~0; rules VOID incentivized traffic. So **primary monetization = A-Ads** (display; but CPM on this traffic is low — modest expectations), with FaucetPay-referral (`r/301005`) + Satsback/affiliate as **passive extras** only.
> **Concept (5 panel rounds):** a **proof-first, utility-first "free crypto" hub** — quality over quantity (~10-12 strong evergreen pages, not thin listicles). Distinctive anti-templated angle = the **"Reality Metric"**: every earning method shows real **$/hour** + payout proof + scam-risk (radical honesty as the brand). Content clusters: Earn (offerwalls/PTC/surveys/learn-to-earn/faucets), Safety (scam checks), Tools (an interactive earnings calculator = link magnet + dwell time), plus `/methodology`, reviews (faucetpay/satsback), Satsback shop (exists). Google-safe: unique copy, labeled ads, proof/timestamps, no cloaked money-pages indexed. Design system = **light-only, teal=earn / amber=shop, mono figures** (validated, v3 mockup). Spec: `docs/superpowers/specs/2026-09-20-referral-hub-design.md`; Phase-0 plan + telemetry: `docs/superpowers/{plans,research}/`.
> **⚠️ Site is currently DOWN / listed for sale.** On 2026-08-22 the NS were switched to `ns1/ns2.afternic.com` → the domain now serves Afternic's for-sale lander (BIN $4,900 / Min-Offer $2,700), the Next.js app on the VPS is unreachable, and the GSC-verify TXT dropped. **To relaunch:** revert NS from Afternic back to Hostinger defaults, restore A/AAAA → VPS `72.62.154.119`, re-add the GSC DNS-TXT, then redeploy. **Delist/withdraw the Afternic listing first** (can't serve the site and the lander at once — reverting NS auto-kills the lander, but withdraw the listing to stop offers/broker flow). Payout was set to PayPal, registrar stays Hostinger.
> Full history of the sell decision (now superseded) + Afternic listing details: project memory `freecrypto-freeze-decision`.

- **App lives on VPS, NOT Business plan.** SSH: `ssh -i ~/.ssh/autodtcs_key root@72.62.154.119`. App: `/home/deploy/freecrypto` (deploy user, pm2 name `fc`, port 3003).
- **Deploy:** `git push origin main` triggers `.github/workflows/deploy.yml` (uses `VPS_SSH_KEY` repo secret). Remote is SSH (`git@github.com:freecryptonet/freecrypto-site.git`) — local PAT auth retired 2026-05-16, key `~/.ssh/github_ed25519`.
- **Manual deploy:** `sudo -u deploy bash -c 'exec 9>/home/deploy/freecrypto.deploy.lock; flock -w 600 9 || { echo "another deploy holds the lock"; exit 1; }; cd /home/deploy/freecrypto && git pull && npm ci && npx next build && pm2 reload fc --update-env'` — the `flock` is required: it shares one lock with CI's `deploy.yml` so a manual deploy can't race an in-flight CI build (that collision failed a run on 2026-07-26). Better yet, if CI is what's stuck, re-run the workflow instead of deploying manually.
- **PORT trap:** `next start` does NOT read `PORT` env var. pm2 entrypoint MUST pass `-p 3003`: `pm2 start npx --name fc -- next start -p 3003`.
- **`NEXT_PUBLIC_*` env vars are baked at build time.** Changes need `next build`, not just `pm2 reload`.
- **Editorial seed edits:** `npm run seed:sql` regenerates `migrations/002_seed_data.sql` → import via `mysql freecrypto < ...` over SSH (not phpMyAdmin anymore — that DB is gone).
- **Indexability gate:** `isAirdropIndexable` in `src/lib/seo.ts` is the single source of truth for what gets into the sitemap + escapes noindex. Threshold = 800 chars combined body. Editing the threshold ripples to both sitemap and per-page metadata.
- **Mobile grid pattern:** every grid container MUST start with `grid-cols-1` before `sm:grid-cols-2 ...` — without it CSS Grid `auto` tracks size to AAds intrinsic 728px width and overflow the viewport.
- **Hostinger DNS MCP `DNS_deleteDNSRecordsV1` schema is broken** (no `filters` param). Use Playwright on hPanel DNS-zone editor for record deletions.
